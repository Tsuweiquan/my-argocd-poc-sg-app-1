apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sg-app.fullname" . }}-test-connection"
  labels:
    {{- include "sg-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sg-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
